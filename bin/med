#!/usr/bin/env node

var program = require('commander');

program.version(require('../package').version)
  .usage('<command> [options]')
  .command('page', 'generate a new page from a template')
  .command('new', 'create new project')
  .parse(process.argv);

module.exports = program;

